/*
	JTSK-320111
	a1 p2.c
	Desar Mejdani
	d.mejdani@jacobs-university.de
*/

#include <stdio.h>

int main() 
{
	int result; /* The result of our calculation */
	result = (2 + 7) * 9 / 3;
	printf("The result is %d\n", result); /* The value of the placeholder was missing. It should be added so that it is printed in the output. */
	return 0;
}
